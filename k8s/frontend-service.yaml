apiVersion: v1
kind: Service
metadata:
  name: dotnet-crud-frontend-service
  namespace: dotnet-app-gcp
spec:
  selector:
    app: dotnet-crud-frontend
  ports:
  - port: 80
    targetPort: 80
  type: LoadBalancer
